# 🧠 Hyperdimensional NCO Array (+ Linear Readout) — مسودة المبدأ

**معمارية حوسبة فائقة الأبعاد تعتمد على مذبذبات NCO قابلة للبرمجة وقراءة خطية (Linear Readout)**

---

## 1. الفكرة الأساسية — بجملة واحدة

**مصفوفة ضخمة من NCOs (N=256–4,096)، كل واحد بوزن مختلف قابل للبرمجة، تعمل بالتوازي الكامل لتحويل أي إشارة داخلة إلى بصمة فريدة (fingerprint) في فضاء N-بعدي. ثم طبقة قراءة خطية (Linear Readout) تحوّل هذه البصمة إلى قرار (فئة) عبر ضرب نقطي مع أوزان مكتسبة.**

```
signal_in ──► ┌───────────────────────────────────┐
              │  NCO Array (N=256, موازٍ كامل)    │
              │  كل NCO i: firing_count_i بعد      │
              │  N_STEPS دورة                     │
              └───────────┬───────────────────────┘
                          │ fingerprint vector F (طول N)
                          ▼
              ┌───────────────────────────────────┐
              │  Linear Readout: S = W_out × F    │
              │  (W_out: N × C, C=عدد الفئات)      │
              │  الفئة = argmax(S)                  │
              └───────────────────────────────────┘
                          │
                          ▼
                       class_id
```

---

## 2. لماذا Linear Readout بدلاً من Hamming/Manhattan distance؟

| المقارنة | المسافات (Hamming/Manhattan) | Linear Readout (مُقترَح) |
|---------|------------------------------|------------------------|
| موارد FPGA | N × C مقارنات + C × N تخزين | N × C FP multipliers (أو int) |
| دورات/تصنيف | C × N (بطيء) | **C (إذا موازي) أو C × N/عدد الـ MACs** |
| تدريب | يحتاج كل prototypes | **LMS / Hebbian بسيط** |
| دقة مقارنة بـ HDC | جيدة | **أفضل (لأن الأوزان مكتسبة)** |

**الميزة الجوهرية:** المسافة تعطي كل الأبعاد وزناً متساوياً. Linear Readout يتعلّم أي الأبعاد مهمة وأيها غير مهمة لكل فئة — أدق وأقل استهلاكاً للموارد.

---

## 3. آلية العمل

### 3.1 أثناء التصنيف

```
لكل إشارة x (دورة ساعة واحدة لكل N_STEPS خطوة):

1. NCO Array (متوازٍ بالكامل)
   لكل i ∈ [0, N-1]:
     F_Word_i = W_i × x          // W_i: وزن NCO i (قابل للبرمجة)
     NCO_i يعمل N_STEPS دورة     // كل NCO يعمل فوراً
     firing_count_i → f_i        // تكامل زمني

   الناتج: متجه F = [f₀, f₁, ..., f_{N-1}]

2. Linear Readout
   S_j = Σ(W_out_{j,i} × f_i)   // لكل فئة j ∈ [0, C-1]
   decision = argmax(S)
```

### 3.2 أثناء التدريب (على مضيف — مرة واحدة)

```
المدخلات: M عينة تدريب مع فئاتها

الخطوة 1: بناء بصمات التدريب
   لكل عينة m:
     F_m = NCO_array(sample_m)    // متجه طول N

الخطوة 2: تدريب Linear Readout
   // يمكن استخدام:
   // (أ) LMS (Least Mean Squares) — مضيف يحسب W_out
   // (ب) Hebbian على الشريحة — W_out += η × F × (target - S)
   // أو ببساطة حل المعادلة: F × W_out = T (انحدار خطي)

   W_out = solve(F_matrix, T_matrix)   // N × C

النتيجة: مصفوفة أوزان W_out جاهزة
```

**ملاحظة:** التدريب يتم على مضيف (حاسوب) أو FPGA خارجي. الـ NCO array لا يتغير بعد التدريب — فقط W_out يُحمّل.

---

## 4. تقدير الموارد على FPGA

### 4.1 NCO Array (N=256)

| المكون | لكل NCO | × 256 |
|--------|---------|-------|
| LUTs (NCO + LFSR + عداد) | ~25 LUTs | **~6,400 LUTs** |
| FFs (accumulator 32-bit + LFSR + عداد) | ~100 FFs | **~25,600 FFs** |

### 4.2 Linear Readout (N=256, C=10)

**الخيار A — متسلسل (Serial):**
- MAC واحد (Multiplier-Accumulator) يعمل دورياً
- C × N = 2,560 دورة ساعة لكل تصنيف
- الموارد: ~100 LUTs فقط

**الخيار B — موازي بالكامل:**
- C = 10 × MACs موازية
- N = 256 دورة لكل تصنيف
- الموارد: ~10 × 200 LUTs = **~2,000 LUTs إضافية**

**الخيار C — مختلط (مُقترَح):**
- P = 32 MACs موازية
- (N/P) × C = 8 × 10 = 80 دورة
- الموارد: ~32 × 200 = **~6,400 LUTs إضافية**

### 4.3 الإجمالي المقدر

| التكوين | LUTs | FFs | دورات/تصنيف | FPGA مناسب |
|---------|------|-----|-------------|-----------|
| N=256, Serial Readout | ~6,500 | ~25,600 | 2,560 + 5 | iCE40UP5K (ممتلئ) |
| N=256, Mix Readout | **~12,800** | **~25,600** | **80 + 5** | **ECP5-12F** |
| N=512, Mix Readout | ~25,000 | ~51,000 | 160 + 5 | ECP5-85F |
| N=1024, Parallel | ~50,000 | ~102,000 | 10 + 5 | Artix-7 200T |

### 4.4 الطاقة التقديرية

```
NCO Array (N=256, 5 دورات): ~256 × 0.12 pJ ≈ ~30 pJ
Linear Readout (80 دورة):   ~80 × 0.5 pJ ≈ ~40 pJ
الإجمالي:                   ~70 pJ لكل تصنيف (10 فئات)
مقابل CMOS:                 ~1,000 pJ لنفس المهمة ← ~14x توفير
```

---

## 5. ميزة القوة — لماذا هذه المعمارية غير مسبوقة؟

| الميزة | القيمة |
|--------|--------|
| **عدد الفئات** | غير محدود — كل فئة تحتاج عمود واحد في W_out (N×C) |
| **عدد المذبذبات** | N=256–4,096 — يحدد قوة التمييز |
| **زمن التصنيف** | ثابت — N_STEPS + (N/P)×C دورة |
| **تدريب** | **مرة واحدة فقط** — LMS على مضيف |
| **نقل التدريب** | W_out فقط (N × C قيمة) — سريع النقل |
| **FPGA ملائم** | **ECP5-12F** للتكوين المختلط المُقترَح |

### مقارنة مع البدائل

| | HD NCO Array | CNN | SVM | HDC تقليدي |
|--|-------------|-----|-----|-----------|
| LUTs | ~12,800 | ~500,000 | N/A | ~8,000 |
| تدريب على FPGA | محدود (W_out فقط) | لا | لا | نعم |
| زمن/تصنيف | ~85 دورة | ~10,000 دورة | ~1,000 دورة | ~100 دورة |
| طاقة | ~70 pJ | ~10 nJ | ~1 nJ | ~100 pJ |
| دقة MNIST | ? | 99% | 98% | 95% |

---

## 6. الدروس من Onyx V2 المطبقة هنا

| الدرس | التطبيق |
|-------|---------|
| كل NCO يحتاج LFSR مستقل | ✅ NCO مدمج مع LFSR (مثل v2.1.0) |
| Uniform noise تكفي | ✅ LFSR مباشر — لا Gaussian |
| N_WINDOW=5 آمنة | ✅ نبدأ بـ 5 ثم نحاول التقليص |
| W_out على مضيف (تدريب خارجي) | ✅ نتجنب التعقيد على الشريحة |
| بصمة كاملة (firing_count) بدلاً من FDE | ✅ لأن multi-class يحتاج معلومات أكثر |

---

## 7. الفجوات المفتوحة

| # | التحدي | الصعوبة |
|---|--------|---------|
| 1 | **N=32 هل يكفي لـ 3 فئات؟** — الخطوة الأولى | 🟢 منخفضة |
| 2 | **كم N_STEPS؟** 2 أم 5؟ | 🟢 منخفضة |
| 3 | **Binarized firing_count أم continuous؟** — يؤثر على مساحة W_out | 🟡 متوسطة |
| 4 | **دقة MNIST مع N=256؟** — الهدف ≥ 90% | 🟡 متوسطة |
| 5 | **Verilog للقراءة الخطية** — MAC موازٍ مقابل متسلسل | 🔴 عالية |

---

## 8. خارطة الطريق

```
المرحلة 1: Python prototype (N=32, C=3) → هل يصل 100%؟
    ↓ (إذا نجح)
المرحلة 2: Python على MNIST (N=256, C=10)
    ↓ (إذا ≥ 90%)
المرحلة 3: Verilog لـ NCO array واحد (قابل للتوسع)
    ↓
المرحلة 4: Linear Readout في Verilog (Mixed parallel)
    ↓
المرحلة 5: Yosys synthesis + CI
```

---

## 9. ما يميز هذا عن "مجرد شبكة"

```
الشبكة العصبية:          x → W₁x → ReLU → W₂x → softmax
HD NCO Array:            x → NCO(x×W_i)∫dt → W_out × F → argmax

الفرق:
  - الشبكة: مضاعفات مصفوفات (DSP) + backpropagation
  - HDNA:   تكامل زمني (LUTs + عدادات) + LMS (على مضيف)
```

NCO يعمل كـ **Reservoir Computing برقمي — مع ذاكرة زمنية مدمجة في كل عقدة**. هذا يسمح له باكتشاف أنماط زمنية لا تكتشفها الشبكات التغذوية الأمامية.

---

## 10. الخطوة التالية (طلب الخبير)

عند الموافقة:

1. Prototype Python بـ **N=32** على **3 فئات** (مثل v3 — σ=0.06)
2. قياس الدقة + تأثير N_STEPS
3. التوسع لاحقاً إلى N=256 و MNIST

---

*"كل NCO هو بُعد في فضاء لا نهائي. كل فئة هي نجم — والقراءة الخطية هي التلسكوب."*
— Iconomputer Architect
